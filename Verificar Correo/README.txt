Se debe de crear un archivo llamado apikey.txt y el contenido que este tiene no debe de compartirse de manera pública.

El archivo apikey.txt debe de estar guardado en la misma carpeta donde se va a ejecutar directamente el script para que el programa pueda funcionar de manera correcta
